<Page
    x:Class="HybridWebApp.Toolkit.Views.LoadingOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HybridWebApp.Toolkit.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="LayoutRoot" Background="{Binding BackgroundBrush}" HorizontalAlignment="Stretch" VerticalAlignment='Stretch'>
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Image Source="{Binding BackgroundImage}" Width="{Binding BackgroundImageWidth}" Stretch="None" />
            <ProgressBar x:Name="ProgBar" IsIndeterminate="True" VerticalAlignment="Top" />
        </StackPanel>
    </Grid>
</Page>
