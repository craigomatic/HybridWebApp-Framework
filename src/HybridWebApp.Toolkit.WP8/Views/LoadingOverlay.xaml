<UserControl x:Class="HybridWebApp.Toolkit.WP8.Views.LoadingOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}">

    <Grid x:Name="LayoutRoot" Background="{Binding BackgroundBrush}" HorizontalAlignment="Stretch" VerticalAlignment='Stretch'>
        <StackPanel Grid.Row="1" VerticalAlignment="Center">
            <Image Source="{Binding BackgroundImage}" Width="{Binding BackgroundImageWidth}" Stretch="None" />
            <ProgressBar x:Name="ProgBar" IsIndeterminate="True" VerticalAlignment="Top" Foreground="{Binding ForegroundBrush}"/>
        </StackPanel>
    </Grid>
</UserControl>